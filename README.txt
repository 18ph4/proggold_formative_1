project uses node so make sure have node install.
run npm install to install dependencies
run npm start to run the server 
go to http://127.0.0.1:8080
inside static is where my CSS, js, HTML, content and csv files are.